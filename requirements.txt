# Core dependencies for profanity filtering (CPU works out of the box)
faster-whisper>=1.2.1  # Primary transcription engine (CTranslate2 backend)
numpy>=1.24.0
yt-dlp>=2024.0.0  # For YouTube video downloads

# Note: FFmpeg must be installed on the system
# Install with: sudo apt install ffmpeg (Linux) or brew install ffmpeg (macOS)
#
# CUDA / cuDNN are OPTIONAL and only needed for NVIDIA GPU acceleration.
# Do not add them as hard Python requirements (breaks CPU-only installs).
# See README "Optional GPU Acceleration (Windows / NVIDIA)" for setup.
